import ListHeader from '../components/list/ListHeader';

const FriendsList = () => {
    return (
        <div>
            <ListHeader title={'친구'} />
        </div>
    );
};

export default FriendsList;
