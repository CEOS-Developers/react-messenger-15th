import styled from 'styled-components';

const Setting = () => {
  return (
    <>
      <SettingContainer>Setting</SettingContainer>
    </>
  );
};

export default Setting;

const SettingContainer = styled.div``;
